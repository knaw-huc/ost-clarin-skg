"""This repository includes a Locust load test at `src/locustfile.py`.

The root-level `locustfile.py` has been moved to `src/locustfile.py` to keep tests with source code.

Run Locust from the repository root by pointing to the new file:

```bash
locust -f src/locustfile.py --host=http://localhost:41012
```
"""

